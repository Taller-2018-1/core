import { Document } from './document';

export class Registry {
  registryID: number;
  dateAdded: Date;
  date: Date;
  name: string;
  value: number;
  urlDocuments: string[];

  constructor() {
  }
}
